import 'package:flutter/material.dart';

class cake extends StatelessWidget
{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Center(child: Text("I am allison cake"),),
    );
  }
}