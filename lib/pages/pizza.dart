import 'package:flutter/material.dart';

class pizza extends StatelessWidget
{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Center(child: Text("I am pizza"),),
    );
  }
}